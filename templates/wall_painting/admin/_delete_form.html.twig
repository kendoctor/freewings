<form method="post" action="{{ path('admin_wall_painting_delete', {'id': wall_painting.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ wall_painting.id) }}">
    <button class="btn">Delete</button>
</form>